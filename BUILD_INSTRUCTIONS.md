# 📱 طريقة بناء تطبيق APK للأندرويد

## المتطلبات الأساسية

### على نظام Linux (يُفضل Ubuntu 20.04 أو أحدث):

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت المتطلبات الأساسية
sudo apt install -y \
    python3 \
    python3-pip \
    python3-venv \
    git \
    zip \
    unzip \
    openjdk-17-jdk \
    autoconf \
    libtool \
    pkg-config \
    zlib1g-dev \
    libncurses5-dev \
    libncursesw5-dev \
    libtinfo5 \
    cmake \
    libffi-dev \
    libssl-dev \
    ccache
```

## طريقة البناء

### 1. تثبيت Buildozer

```bash
# إنشاء بيئة افتراضية
python3 -m venv buildenv
source buildenv/bin/activate

# تثبيت Buildozer و Cython
pip install --upgrade pip
pip install buildozer cython==0.29.33
```

### 2. الانتقال لمجلد المشروع

```bash
cd medical_records_app
```

### 3. بناء التطبيق

```bash
# بناء APK (المرة الأولى تأخذ وقت طويل - قد تصل لساعة)
buildozer android debug

# أو لبناء نسخة release:
buildozer android release
```

### 4. العثور على ملف APK

بعد اكتمال البناء، ستجد ملف APK في:
```
bin/medicalrecords-1.0-arm64-v8a-debug.apk
```

## نقل التطبيق للهاتف

### عبر USB:
```bash
adb install bin/medicalrecords-1.0-arm64-v8a-debug.apk
```

### أو انسخ الملف يدوياً:
- انسخ ملف APK من مجلد `bin/` إلى هاتفك
- افتح الملف من هاتفك وثبته

## حل المشاكل الشائعة

### 1. مشكلة في SDK أو NDK:
```bash
buildozer android clean
rm -rf .buildozer
buildozer android debug
```

### 2. خطأ في الذاكرة:
أضف في ملف `buildozer.spec`:
```ini
android.gradle_options = org.gradle.jvmargs=-Xmx2048m
```

### 3. بطء البناء:
```bash
# تفعيل ccache لتسريع البناء
export USE_CCACHE=1
export NDK_CCACHE=ccache
```

## البناء على Windows

يُفضل استخدام **WSL2** (Windows Subsystem for Linux):

```powershell
# في PowerShell
wsl --install
```

ثم اتبع نفس خطوات Linux داخل WSL.

## اختبار التطبيق

بعد التثبيت على الهاتف:
1. افتح التطبيق "نظام الملفات الطبية"
2. انتظر قليلاً حتى يتم تحميل الخادم
3. ستفتح الواجهة تلقائياً
4. التطبيق يعمل بالكامل أوف لاين!

## ملاحظات مهمة

- 🔴 البناء الأول يأخذ وقت طويل (30-60 دقيقة)
- ✅ البناء التالي أسرع بكثير
- 💾 تأكد من وجود مساحة كافية (5+ GB)
- 🌐 تحتاج اتصال إنترنت فقط أثناء البناء
- 📱 التطبيق النهائي يعمل أوف لاين بالكامل

## حجم التطبيق

- التطبيق النهائي: ~50-80 MB
- يشمل: Python + Flask + جميع المكتبات + قاعدة البيانات

## الأذونات المطلوبة

التطبيق يطلب هذه الأذونات:
- ✅ التخزين: لحفظ قاعدة البيانات
- ✅ الميكروفون: للإدخال الصوتي
- ✅ الإنترنت: للميزات المستقبلية (اختياري)

## التحديثات المستقبلية

لتحديث التطبيق:
1. عدل الملفات
2. ارفع رقم الإصدار في `buildozer.spec`
3. أعد البناء: `buildozer android release`

---

## بديل سريع: استخدام Python for Android مباشرة

إذا واجهت مشاكل مع Buildozer:

```bash
pip install python-for-android
p4a create --requirements=python3,flask,kivy --arch=arm64-v8a --permission INTERNET
p4a build --bootstrap=sdl2 --requirements=python3,flask,kivy
```

---

## الدعم

للمشاكل أو الاستفسارات:
- راجع سجلات البناء في `.buildozer/logs/`
- جرب: `buildozer -v android debug` للحصول على معلومات مفصلة

صنع بـ ❤️ باستخدام Kivy و Buildozer

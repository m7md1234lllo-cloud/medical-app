# 🚀 بناء APK تلقائياً باستخدام GitHub Actions

## نظرة عامة

GitHub Actions راح يبني APK تلقائياً كل ما ترفع تحديث للكود. البناء يصير في سيرفرات GitHub (مجاناً!) ويعطيك APK جاهز للتحميل.

---

## ⏱️ معلومات مهمة

- ⏳ **مدة البناء**: 20-40 دقيقة
- 💰 **التكلفة**: مجاني 100%
- 🔄 **تلقائي**: يبني عند كل push
- 📦 **الناتج**: ملف APK جاهز

---

## 📋 الخطوات (من Termux)

### 1. تثبيت Git على Termux

```bash
pkg update
pkg install git
```

### 2. إنشاء حساب GitHub (إذا ما عندك)

- روح: https://github.com
- اضغط "Sign up"
- سوي حساب جديد (مجاني)

### 3. إنشاء Repository جديد

**من الجوال/المتصفح:**

1. افتح GitHub
2. اضغط "+" في الأعلى → "New repository"
3. اسم الـ repo: `medical-records-app`
4. خل الـ repo **Public** (مجاني)
5. اضغط "Create repository"

### 4. رفع المشروع من Termux

```bash
# انتقل لمجلد المشروع
cd ~/downloads/medical_records_app

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit - Medical Records App"

# ربط الـ repo (غير اسم المستخدم)
git remote add origin https://github.com/YOUR_USERNAME/medical-records-app.git

# رفع الملفات
git branch -M main
git push -u origin main
```

**ملاحظة:** راح يطلب منك username و password:
- Username: اسم مستخدمك في GitHub
- Password: استخدم **Personal Access Token** بدلاً من كلمة المرور

### 5. إنشاء Personal Access Token

إذا طلب كلمة مرور:

1. اذهب لـ: https://github.com/settings/tokens
2. اضغط "Generate new token" → "Generate new token (classic)"
3. أعطيه اسم: `Termux Access`
4. اختر scope: `repo` (فقط)
5. اضغط "Generate token"
6. **انسخ الـ token** (ما راح تشوفه مرة ثانية!)
7. استخدمه بدلاً من كلمة المرور عند git push

---

## 🎯 البناء التلقائي

### بعد رفع الكود:

1. **تلقائياً**: GitHub Actions راح يبدأ البناء
2. راقب التقدم: اذهب لـ repo → تبويب "Actions"
3. راح تشوف workflow اسمه "Build Android APK"
4. اضغط عليه لمشاهدة التقدم

### تحميل APK:

بعد اكتمال البناء (20-40 دقيقة):

1. اذهب لـ workflow اللي اكتمل (علامة ✅ خضراء)
2. انزل لقسم "Artifacts"
3. اضغط على "medical-records-apk"
4. راح يحمل ملف ZIP فيه APK
5. فك الضغط وثبت APK على هاتفك!

---

## 🔄 تشغيل البناء يدوياً

إذا تبي تبني بدون رفع كود جديد:

1. اذهب لـ repo → "Actions"
2. اختر "Build Android APK"
3. اضغط "Run workflow"
4. اختر branch: `main`
5. اضغط "Run workflow"
6. انتظر البناء يخلص

---

## 📝 تحديث الكود ورفع نسخة جديدة

كل ما تبي تحديث التطبيق:

```bash
# عدل الملفات اللي تبيها
nano app.py
# أو أي ملف آخر

# أضف التعديلات
git add .

# عمل commit
git commit -m "وصف التحديث"

# رفع التحديث
git push origin main
```

**تلقائياً**: راح يبني APK جديد!

---

## 🏷️ إصدار Release رسمي

لعمل نسخة Release مرقمة:

```bash
# أولاً، عدل رقم الإصدار في buildozer.spec
nano buildozer.spec
# غير version = 1.0 لـ version = 1.1 مثلاً

# ثم:
git add buildozer.spec
git commit -m "v1.1: وصف التحديثات"
git tag v1.1
git push origin main
git push origin v1.1
```

الـ APK راح يظهر في صفحة "Releases" تلقائياً!

---

## 📊 معلومات إضافية

### استهلاك GitHub Actions (مجاني):

- الحسابات المجانية: **2000 دقيقة شهرياً**
- كل build يأخذ: ~30 دقيقة
- تقدر تبني: **~60 مرة شهرياً**
- أكثر من كافي!

### Cache (التخزين المؤقت):

- GitHub راح يحفظ ملفات البناء
- البناء الثاني أسرع: ~15-20 دقيقة
- يوفر وقت ومصادر

---

## 🔧 استكشاف الأخطاء

### البناء فشل ❌

**الحل:**

1. اذهب للـ workflow الفاشل
2. اضغط على الخطوة اللي فشلت
3. اقرأ رسالة الخطأ
4. أغلب المشاكل:
   - أذونات: تأكد من permissions في الـ repo
   - ملفات ناقصة: تأكد من رفع كل الملفات
   - buildozer.spec: تحقق من الإعدادات

### رسالة خطأ في git push

**الحل:**

```bash
# تحديث الـ repo
git pull origin main --rebase

# ثم حاول مرة ثانية
git push origin main
```

### لا أستطيع تحميل Artifact

**الحل:**

- تأكد من تسجيل دخولك في GitHub
- جرب من متصفح آخر
- أو: انتظر Release tag (إذا سويت tag)

---

## 💡 نصائح مهمة

### للسرعة:

1. ✅ استخدم cache (مفعل تلقائياً)
2. ✅ لا ترفع ملفات كبيرة (مثل .buildozer/)
3. ✅ استخدم `.gitignore` (موجود)

### للأمان:

1. 🔒 لا ترفع كلمات السر أو tokens
2. 🔒 لا ترفع ملفات قاعدة البيانات مع بيانات حقيقية
3. 🔒 استخدم Private repo إذا كان الكود حساس

### للتنظيم:

1. 📝 اكتب commit messages واضحة
2. 🏷️ استخدم tags للإصدارات المهمة
3. 📋 استخدم Issues لتتبع المشاكل

---

## 📱 مثال كامل (من البداية للنهاية)

### في Termux:

```bash
# 1. تهيئة
cd ~/downloads/medical_records_app
git init
git add .
git commit -m "Initial commit"

# 2. ربط GitHub (غير YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/medical-records-app.git

# 3. رفع
git branch -M main
git push -u origin main
```

### في GitHub:

1. انتظر 5 دقائق
2. اذهب لـ "Actions"
3. شاهد البناء يشتغل (20-40 دقيقة)
4. بعد الانتهاء: حمل APK من Artifacts
5. ثبته على هاتفك!

---

## 🎉 النجاح!

الآن عندك:
- ✅ كود في GitHub (آمن ومحفوظ)
- ✅ بناء تلقائي لـ APK
- ✅ نسخ احتياطية تلقائية
- ✅ سهولة المشاركة مع الآخرين

---

## 📞 مساعدة إضافية

### مشكلة في Git؟

```bash
# تكوين Git لأول مرة
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### مشكلة في Authentication؟

- استخدم SSH بدلاً من HTTPS
- أو استخدم GitHub CLI: `pkg install gh`

### أسئلة أخرى؟

- راجع: https://docs.github.com/actions
- أو اسأل في Issues

---

## 🚀 التالي

بعد ما تتعود على العملية:
- جرب workflows أخرى
- أضف tests تلقائية
- سوي notifications
- شارك المشروع!

---

صُنع بـ ❤️ باستخدام GitHub Actions
حظاً موفقاً! 🎊
